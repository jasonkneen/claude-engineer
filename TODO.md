TODO

1, Overhaul architecture to create a reactJS / nextJS frontend, turn the app.py into a backend API to inegrate with it.

2. Update the web inteface to show messages, markdown, code rendering, scrolling interface

3. Floating chat area at the bottom, screenshots attached that show some sample designs etc.

4. Tooling status needs to be monitored and shown (see images), so we can see status of project, tasks, tools / agents

5. Can agents be tools? Are they already?

This needs to be a full agentic system that can be given a task to do in natural language and it does it, it should also be capable of writing agent code in other systems like crewAI, langchain, openai swarm etc.
